/**
 * Recibe una URL y retorna su informacion (API o JSON)
 * @param {string} url 
 * @returns información con los datos para crear los niveles
 */

const getData = (url) => {
    return fetch(url)
}